#!/usr/bin/env python3

language = "Python"
version = 3
pi_value = 3.14159
pi_format = f"{pi_value:.2f}"
is_valid = pi_value > 3

print(f"Language: {language}")
print(f"Version: {version}")
print(f"Pi approx: {pi_format}")
print(f"Computation valid: {is_valid}")
